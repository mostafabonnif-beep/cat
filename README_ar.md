# OUSSAMA Cutter
[![CI](https://github.com/mostafabonnif-beep/cat/actions/workflows/ci.yml/badge.svg)](https://github.com/mostafabonnif-beep/cat/actions/workflows/ci.yml)
[![License: GPL v3](https://img.shields.io/badge/License-GPLv3-blue.svg)](LICENSE)
[![Tests](https://img.shields.io/badge/tests-855%20passed-brightgreen)](tests/)

**البديل المفتوح المصدر المجاني 100% لـ Opus Clip — يعمل محلياً وبلا حدود**
حوّل فيديوهات يوتيوب الطويلة إلى مقاطع قصيرة فيروسية جاهزة لـ TikTok وInstagram Reels وYouTube Shorts — بذكاء اصطناعي متقدم، وترجمات ديناميكية، وتتبع دقيق للوجه، وترجمة تلقائية. كل شيء يعمل على جهازك.

[![Stars](https://img.shields.io/github/stars/mostafabonnif-beep/cat?style=social)](https://github.com/mostafabonnif-beep/cat/stargazers)
[![Forks](https://img.shields.io/github/forks/mostafabonnif-beep/cat?style=social)](https://github.com/mostafabonnif-beep/cat/network/members)

[English](README_en.md) • [Português](README.md) • **العربية**
> **الإسناد / الاعتماد** — هذا المشروع فرع (fork) من مشروع
> [ViralCutter](https://github.com/RafaelGodoyEbert/ViralCutter) مفتوح
> المصدر للمطوّر [Rafael Godoy](https://github.com/RafaelGodoyEbert)
> (رخصة GPL-3.0). يحتفظ برخصة GPL-3.0 الأصلية ويضيف فوق الكود الأصلي
> الإصلاحات وطبقة الأمان والواجهة والتغليف الموجودة في هذا المستودع.


## لماذا OUSSAMA Cutter "يغيّر قواعد اللعبة"؟

انسَ الاشتراكات الغالية وحدود الدقائق. OUSSAMA Cutter يعطيك قوة غير محدودة على جهازك أنت.

| الميزة | OUSSAMA Cutter (مفتوح المصدر) | Opus Clip / Klap / Munch (خدمات سحابية) |
| :--- | :--- | :--- |
| **السعر** | **مجاني وبلا حدود** | $20–$100 شهرياً + حدود دقائق |
| **الخصوصية** | **محلي 100%** (بياناتك لا تغادر جهازك) | رفع لسيرفرات طرف ثالث |
| **الذكاء الاصطناعي** | **مرن**: Gemini (مجاني)، GPT-4، **GGUF محلي (أوفلاين)** | ما يقدمونه هم فقط |
| **تتبع الوجه** | **شاشة مقسمة (وجهان)**، متحدث نشط (تجريبي)، تلقائي | أساسي أو مدفوع إضافي |
| **الترجمة** | **نعم** (ترجم لأكثر من 10 لغات) | إمكانيات محدودة |
| **التحرير** | **تصدير XML لـ Premiere Pro** (تجريبي) | محرر ويب محدود |
| **العلامة المائية** | **صفر** | موجودة (في الخطط المجانية) |

**نتائج احترافية، خصوصية كاملة، وتكلفة صفر.**

## الميزات الرئيسية 🚀

- 🤖 **قص فيروسي بالذكاء الاصطناعي**: يحدد تلقائياً الخطافات (hooks) وأكثر اللحظات تفاعلاً باستخدام **Gemini** أو **GPT-4** أو **نماذج محلية (Llama 3، DeepSeek، إلخ)**.
- 🛡️ **فلتر الأمان ضد مخالفات يوتيوب (جديد!)**: يفحص نص كل مقطع مقترح و**يمنع تلقائياً** المقاطع التي تحتوي على **الكلام الذي يحضّ على الكراهية** — إما بحذف المقطع أو **بكتم الكلمة المخالفة فقط (Bleep)** مع الإبقاء على المقطع. طبقة إضافية بمراجعة سياقية عبر الذكاء الاصطناعي + تقرير `safety_report.json` لكل مشروع.
- 🗣️ **نسخ صوتي فائق الدقة**: مبني على **WhisperX** مع تسريع GPU وترجمة مثالية التوقيت، مع `faster-whisper` كمسار احتياطي مستقل.
- 🔊 **فحص جودة الصوت (Audio QC)**: يقيس loudness وtrue peak والصمت في الملفات النهائية عبر FFmpeg، ويكتب `audio_qc_report.json` ويمنع الرفع الحقيقي عند وجود مشكلة تحتاج مراجعة.
- 🎨 **ترجمات ديناميكية**: بأسلوب "Hormozi" مع تمييز كلمة بكلمة، ألوان نابضة، وتخصيص كامل (خط، لون، إطار، ظل).
- 🎥 **إخراج كاميرا تلقائي**:
  - **قص تلقائي 9:16**: يحوّل الأفقي إلى عمودي مع الحفاظ على التركيز.
  - **شاشة مقسمة ذكية**: يكتشف شخصين يتحدثان ويقسم الشاشة تلقائياً.
  - **المتحدث النشط (تجريبي)**: الكاميرا تنتقل لمن يتكلم.
- 🌍 **ترجمة الفيديو**: أنشئ ترجمات مترجمة تلقائياً (مثال: فيديو إنجليزي ← ترجمة عربية).
- 💾 **جودة وتحكم**: اختر الدقة (حتى 4K/الأفضل)، نسّق المخرجات، واحفظ إعدادات المعالجة كقوالب.
- ⚡ **الأداء**: نسخ صوتي بتقنية "slicing" (يعالج مرة واحدة، يقص عدة مرات) وتثبيت سريع جداً عبر `uv`.
- 🖥️ **واجهة حديثة**: WebUI بـ Gradio، وضع داكن، معرض مشاريع، ومحرر ترجمات مدمج — **بواجهة عربية كاملة (RTL)**.

## الواجهة العربية

الواجهة تأتي بالعربية افتراضياً مع دعم كامل للاتجاه من اليمين لليسار. لتشغيلها بلغة أخرى:

```bash
# Linux / macOS
VIRALCUTTER_LANG=en_US python webui/app.py

# Windows (PowerShell)
$env:VIRALCUTTER_LANG="en_US"; python webui/app.py
```

اللغات المتاحة: `ar_SA` (العربية)، `en_US` (الإنجليزية)، `pt_BR` (البرتغالية)، `tr_TR` (التركية).

## مركز التحكم عبر Telegram

يمكن تفعيل مركز Telegram اختيارياً للتحكم في **طابور المعالجة المحلي** أثناء بقاء OUSSAMA Cutter مفتوحاً. يعمل الاتصال من Windows عبر long polling دون webhook أو منفذ عام، وتبقى الفيديوهات وملفات `VIRALS` وOAuth على الجهاز. لا يقبل البوت Bot Token أو `client_secrets.json` أو ملفات أو أوامر shell، ولا ينفذ `/upload` أو `/publish`.

الإعداد env-only: أنشئ Bot من [@BotFather](https://telegram.me/BotFather)، ثم انسخ `telegram_control.example.ps1` إلى `telegram_control.local.ps1` خارج Git وZIP وشغّله محلياً. لا تضع Token في README أو GitHub أو المحادثة. راجع [دليل Telegram العربي](docs/TELEGRAM_CONTROL_AR.md) و[قائمة قبول Windows](docs/RELEASE_CHECKLIST_WINDOWS.md) للأوامر والاختبارات والاستكشاف.

## 🪟 التثبيت على ويندوز (خطوة بخطوة)

> **المتطلبات**: ويندوز 10/11 + إنترنت. (بايثون يُثبَّت تلقائياً بواسطة السكربت)

**الطريقة 1 — سكربت واحد (موصى بها):**
```bat
:: 1) نزّل المشروع (زر Code ← Download ZIP وفك الضغط، أو:)
git clone https://github.com/mostafabonnif-beep/cat.git
cd ViralCutter

:: 2) شغّل المثبّت (يثبّت بايثون/الاعتماديات/FFmpeg/whisperx — اسألك عن كرت الشاشة)
install_dependencies.bat

:: 3) ضع مفتاح Gemini (مرة واحدة)
setx GEMINI_API_KEY "مفتاحك-هنا"

:: 4) شغّل البرنامج
run.bat
:: أو الواجهة الرسومية:
run_webui.bat
```

**الطريقة 2 — يدوياً (PowerShell):**
```powershell
winget install Python.Python.3.11          # أو من python.org (فعّل "Add to PATH")
winget install Gyan.FFmpeg                 # أو نزّل من gyan.dev
py -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
pip install -r requirements-transcribe.txt # WhisperX + Torch (GPU: ثبّت Torch CUDA أولاً)
# اختياري: مسار مستقل عند تعطل WhisperX أو تعارض Hugging Face
pip install -r requirements-transcribe-fallback.txt
$env:GEMINI_API_KEY="مفتاحك-هنا"
python main_improved.py --url "..." --platform tiktok --polish on
```

**أمثلة تشغيل:**
```bat
:: CLI بمقطع فيروسي + قالب تيك توك + تحسين احترافي
python main_improved.py --url "https://youtube.com/watch?v=..." --platform tiktok --polish on --logo logo.png

:: الواجهة الرسومية (عربية RTL)
run_webui.bat
```

> 🔒 **فيديو خاص أو مقيد عمرياً؟** استعمل كوكيز متصفحك (الفيديو يجب أن يكون متاحاً لحسابك):
> ```bat
> python main_improved.py --url "..." --cookies-from-browser chrome
> :: أو ملف كوكيز مُصدَّر (Netscape):  --cookies cookies.txt
> ```

> 💡 **ملاحظات ويندوز**:
> - بطاقة NVIDIA → اختر CUDA في المثبّت (أسرع بكثير). AMD/Intel → نسخة CPU (تعمل لكن أبطأ).
> - لو فشل `install_dependencies.bat` في البداية، أعد فتح الطرفية بعد تثبيت uv.
> - مفتاح Gemini مجاني من https://aistudio.google.com/apikey

## التثبيت المحلي (سريع جداً ⚡)

> ✅ **للعمل الكامل (يوتيوب → مقطع فيروسي) تحتاج الخطوات التالية**:
> 1. `pip install -r requirements.txt`
> 2. `pip install -r requirements-transcribe.txt`  ← المسار الأساسي (WhisperX + Torch، يُفضّل GPU)
> 3. عند تعطل المسار الأساسي: `pip install -r requirements-transcribe-fallback.txt`  ← faster-whisper مستقل
> 4. ضع مفتاح Gemini: `export GEMINI_API_KEY=...`  (أو `python -m scripts.secure_config --set KEY --passphrase ...`)
>
> في Windows يمكن فرض المسار الاحتياطي مؤقتاً: `$env:VIRALCUTTER_TRANSCRIPTION_BACKEND="faster-whisper"`. الوضع الافتراضي `auto` يفضّل WhisperX ثم يستخدم fallback عند غيابه.
>
> ثم: `python main_improved.py --url "..." --platform tiktok --polish on`

> لفحص جودة صوت مشروع موجود قبل النشر:
> `python -m scripts.audio_qc --project "D:\SS\VIRALS\اسم_المشروع"`
>
> (الرفع المباشر لليوتيوب: `pip install -r requirements-upload.txt` — راجع قسم بوابة الرفع)



### المتطلبات الأساسية

1. **أدوات بناء Visual Studio (C++ Build Tools)**
   مطلوبة لترجمة `insightface` وتجنب أخطاء C++.
   - حمّل [Microsoft C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/).
   - في المثبّت اختر **"Desktop development with C++"**.

2. **Python (يفضل 3.10.x أو 3.11.x)**
   - من [python.org/downloads](https://www.python.org/downloads/).
   - ⚠️ **مهم جداً:** في أول شاشة تثبيت فعّل خيار **"Add Python to PATH"**.

3. **FFmpeg** (معالجة الصوت والفيديو)
   - أسرع طريقة على Windows: `winget install ffmpeg`
   - تحقق بالأمر `ffmpeg -version`.

4. **تعريفات كرت الشاشة (NVIDIA)**
   - حدّث التعريفات لاستخدام تسريع CUDA. كرت NVIDIA **موصى به بشدة** للسرعة والنماذج المحلية.

### خطوات التثبيت

1. **ثبّت الاعتماديات بالسكربت** — افتح مجلد OUSSAMA Cutter وشغّل أحد المثبّتين بالضغط المزدوج:
   - `install_dependencies.bat`: التثبيت **القياسي** (موصى به) — يستخدم Gemini/GPT-4 عبر الإنترنت.
   - `install_dependencies_advanced_LocalLLM.bat`: تثبيت **متقدم** — لتشغيل نماذج أوفلاين 100% (يتطلب كرت شاشة قوي).

2. **إعداد الذكاء الاصطناعي (اختياري)**
   - **Gemini (موصى به/مجاني)**: أضف مفتاحك في `api_config.json`.
   - **محلي (GGUF)**: ضع نماذج `.gguf` في مجلد `models/` وسيكتشفها تلقائياً.

3. **التشغيل**
   - `run_webui.bat` لفتح الواجهة في المتصفح.
   - أو `python main_improved.py` لواجهة سطر الأوامر.

## 🛡️ فلتر الأمان (تجنّب مخالفات "الكلام الذي يحضّ على الكراهية")

أكبر سبب لضربات يوتيوب (Strikes) عند نشر المقاطع القصيرة هو مقطع واحد يحتوي على شتيمة أو تحريض. OUSSAMA Cutter يحميك بثلاث طبقات:

1. **وقاية من المصدر**: البرومبت المُرسل للذكاء الاصطناعي يمنعه صراحةً من اختيار مقاطع فيها خطاب كراهية أو تحريض على العنف.
2. **فلتر الكلمات المحلية قبل القص**: يُفحص نص كل مقطع ضد قائمة محلية متعددة اللغات (عربي فصحى ولهجات، إنجليزي، برتغالي، فرنسي، إسباني، تركي). يفهم التحايل: التشكيل، التطويل (حـــرم)، الأرقام والرموز (@، 3)، و"ال" التعريف الملتصقة.
3. **مراجعة سياقية بالذكاء الاصطناعي (جديد!)**: بعد الفلترة، تُرسل المقاطع الناجية لـ Gemini/G4F بسؤال "هل هذا يخالف سياسة يوتيوب؟" — يكشف الكراهية **السياقية** بدون كلمات محظورة (مثال: "هذه الفئة لا تستحق الوجود"). يعمل تلقائياً ولا يكسر المعالجة عند فشله.

**الأوضاع الأربعة** (من الواجهة أو `--safety-mode`):

| الوضع | السلوك |
| :--- | :--- |
| `block` (افتراضي) | **حذف** المقطع المخالف من قائمة القص نهائياً |
| `censor` 🔇 | **كتم الكلمة المخالفة فقط** (Bleep): يكمّم الصوت في تلك اللحظة + يخفي الكلمة من الترجمة (`████`) — والمقطع الفيروسي يبقى! |
| `flag` | الإبقاء على المقطع مع **وسمه** في التقرير للمراجعة اليدوية |
| `off` | تعطيل الفلتر |

**المخرجات**: 
- `safety_report.json` — حالة كل مقطع: safe/blocked/flagged، الكلمات المطابقة، التصنيف، الخطورة، التوقيت التقريبي + نتائج مراجعة الذكاء الاصطناعي.
- `censor_map.json` (وضع الكتم) — كل كلمة مكتومة وتوقيتها بالضبط.
- تبويب **مراجعة المقاطع** في الواجهة يعرض عمود "الأمان" (✅ / ⚠️ / 🔇 / 🤖⚠️).

**تخصيص القوائم**: انسخ `safety_terms.example.json` إلى `safety_terms.json`:
- `extra_terms` — كلمات إضافية للحظر.
- `allow_terms` — استثناء كلمات من الحظر (لإصلاح الحظر الخاطئ، مثلاً قناة تاريخية تقول "منغولي").

**🔄 تحديث تلقائي لقائمة الكلمات (جديد!)**:
يوتيوب يحدّث سياساته وكلمات التحريض باستمرار — لذلك OUSSAMA Cutter يتحدّث قائمته بنفسه:
- تُنشر القائمة الرسمية `safety_blocklist.json` في مستودع GitHub، وكل جهاز يسحبها **تلقائياً مرة كل 24 ساعة** أثناء المعالجة (أو زر "🔄 تحديث قائمة كلمات الأمان" في الواجهة).
- **بدون إنترنت؟** لا مشكلة — يبقى على آخر قائمة محفوظة (أو المدمجة) ويستمر العمل.
- أي كلمة جديدة تُضاف للمستودع تصل لجهازك بدون تحديث البرنامج نفسه.
- لإيقاف التحديث التلقائي: `--safety-autoupdate off`.

## 📊 بطاقة المخاطر (Risk Scorecard) — ضد المحتوى المُعاد استخدامه وخسارة الربح

بعد كل معالجة، تُنتج الأداة **تقرير امتثال لكل مقطع** (ملف `risk_scorecard.json`) يحسب:

| المحور | ماذا يكشف |
| :--- | :--- |
| **reuse** (إعادة الاستخدام) | نسبة تطابق المقطع النهائي مع المصدر الأصلي (مقارنة فريمات بـ dHash) — **أعلى من 70% = خطر "محتوى مُعاد استخدامه"** يهدد الربح أو يسبب Claim حقوق |
| **first7s** (الربحية) | وجود كلمة مخالفة في **أول 7 ثوانٍ** = إعلانات محدودة |
| **visual** (بصري) | أشرطة سوداء (مظهر منقول) + جاهزية لفحص نموذج محلي للصور |
| **overall** | المستوى الكلي: low / medium / high / danger |

**بوابة النشر**: `--risk-gate warn` (افتراضي) يحذرك ويكتب `publish_blocklist.json` بقائمة المقاطع الممنوعة من النشر؛ و`--risk-gate block` يوقف المعالجة نهائياً عند وجود مقاطع خطرة. يدوياً: `python scripts/risk_scorecard.py --project tmp/مشروعك --exit-on-blocked`.

## ⚠️ دليل عملي: كيف تتجنب مشكلة "المحتوى المُعاد استخدامه"؟

يوتيوب يوقف الربح (وأحياناً يقبل Claims) على المقاطع **غير المحوّلة** — أي قصّتها كما هي بدون إضافة. لحماية قناتك:

1. **لا تنشر مقطعاً "خاماً" من المصدر** — أي مقطع تتجاوز نسبة تطابقه مع المصدر 70% ممنوع. الأداة الآن تكشف هذا بنفسها.
2. **حوّل المحتوى دائماً** (هذا هو معيار يوتيوب الفعلي):
   - ترجمات حية (OUSSAMA Cutter يضيفها ✅)
   - قص/زوم ديناميكي على الوجوه (مفعّل ✅)
   - **تعليق صوتي أو تحليل خاص بك** فوق اللقطات (الأقوى — افعله خارجياً أو عبر أداة تسجيل)
   - مقطع Intro/Outro خاص بقناتك + علامة مائية (شعارك)
3. **لا تقص المصدر نفسه عدة مرات** — تكرار نفس المصدر في مقاطع كثيرة = إشارة "مُعاد استخدامه" حتى مع التحويل.
4. **قصّر المقاطع** (15-45 ثانية) — المقاطع الطويلة الخام أكثر عرضة للـ Claim.
5. **اختر مصادر قابلة للاستخدام**: فيديوهاتك أنت، محتوى Creative Commons، أو فيديوهات بتصريح واضح. المحتوى التجاري (أفلام، قنوات إخبارية) = خطر Claim شبه مؤكد.
6. **أضف سياقاً تعليمياً/تعليقياً** — يوتيوب يسمح بالاقتباس مع تعليق/نقد، وليس النسخ المجرد.
7. **تجنب اللقطات المحورية للمصدر** (المشاهد "التي لا تُنسى") — يوتيوب يتتبعها بدقة.

> الأداة تساعدك بـ: درجة التطابق (reuse score) لكل مقطع + قائمة `publish_blocklist.json` بالمقاطع الممنوعة — لكن **قرار "كيف أحوّل المحتوى" يبقى عليك**، والدليل أعلاه هو خلاصة ما تطلبه يوتيوب.

**من سطر الأوامر**:

```bash
# حذف المقاطع المخالفة (افتراضي)
python main_improved.py --url <URL> --safety-mode block

# كتم الكلمات المخالفة بدل حذف المقطع
python main_improved.py --url <URL> --safety-mode censor

# تعطيل المراجعة بالذكاء الاصطناعي
python main_improved.py --url <URL> --safety-ai off

# فلترة مشروع موجود بدون إعادة معالجة
python scripts/safety_filter.py --project tmp/MyProject --mode block --in-place
```

> **ملاحظة**: وضع الكتم يحتاج ترجمة بمستوى الكلمة (`input.json` من WhisperX) — أي نسخة صوتية عبر Whisper. إذا استخدمت ترجمات يوتيوب الجاهزة فالكتم يُتخطى تلقائياً (وضع `block` يظل يعمل).

## 🎬 جولة v6 (2026-08) — التوزيع + الحماية البصرية + المونتاج الاحترافي

- [x] بناء ملف واحد (`packaging/viralcutter.spec` + سكربتات Win/Linux/macOS) — **1.1**
- [x] تحديث تلقائي للبرنامج (`scripts/auto_updater.py` — يفحص GitHub Releases) — **1.2**
- [x] مثبّتات Linux/macOS (`install_linux.sh` / `install_macos.sh` / `run.sh`) — **1.3**
- [x] فحص بصري محلي بنموذج ONNX (NudeNet-lite) — `python -m scripts.visual_check --download` — **2.1**
- [x] بوابة رفض إجبارية قبل الرفع — `python -m scripts.upload_gate --project <مجلد> --index 0` — **2.2**
- [x] فحص الكابشن/العنوان/الهاشتاغات (Metadata Compliance) — **2.4**
- [x] حذف الصمت والحشو (Jump Cuts) + زوم Punch-in + موسيقى مع Auto-Duck + ووترمارك/Intro/Outro
      عبر أمر واحد: `python main_improved.py --url ... --polish on --logo logo.png` — **3.1–3.4**
- [x] استئناف ذكي بعد الانقطاع (`--checkpoint on`) + حماية GPU من نفاد الذاكرة — **4.1/4.2**
- [x] مفتاح API مشفّر (`python -m scripts.secure_config --set KEY --passphrase ...`) — **4.4**
- [x] 3 عناوين A/B لكل مقطع (في المخرجات: `alt_titles`/`alt_captions`) — **5.3**

أمثلة سريعة:
```bash
# قالب منصة + مونتاج احترافي + فحص بصري
python main_improved.py --url "..." --platform tiktok --polish on \
    --logo logo.png --music music/bed.m4a --auto-download-visual

# بوابة الرفع: ترفض أي مقطع محظور (رمز خروج 3)
python -m scripts.upload_gate --project VIRALS/مشروعك --index 0 --title "..." --caption "..." --hashtags "#shorts"
```

## خارطة الطريق

- [x] إطلاق الكود
- [x] دعم شخصين (شاشة مقسمة)
- [x] ترجمات مخصصة وحرق على الفيديو
- [x] نماذج ذكاء اصطناعي محلية 100% (GGUF)
- [x] ترجمة تلقائية للترجمات
- [x] تتبع ديناميكي للوجه
- [x] تصدير XML لـ Premiere Pro (تجريبي)
- [x] واجهة عربية كاملة (RTL) + دعم 4 لغات
- [x] حزمة اختبارات + CI
- [x] فلتر أمان ضد خطاب الكراهية (حماية من ضربات يوتيوب)
- [x] كتم الكلمات المخالفة (Bleep) + مراجعة سياقية بالذكاء الاصطناعي
- [x] موسيقى خلفية تلقائية (Auto-Duck) — عبر `--polish`
- [x] رفع مباشر لـ TikTok/Instagram (+ YouTube عبر OAuth) — v6.10+
- [x] أبعاد إخراج إضافية (4:5، 1:1، 16:9) — عبر `--output-aspect` (v6.13)
- [x] ووترمارك اختياري — عبر `--polish --logo`

## الفحص المسبق: كل شيء في مكانه قبل التشغيل (v6.12+)

قبل أن يبدأ البرنامج (CLI أو الواجهة) **يفحص كل شيء** — بايثون، ffmpeg/ffprobe،
كل الحزم، `api_config.json`، الخطوط، قائمة الأمان، الترجمات والمجلدات — و
**يثبّت/يصلّح تلقائياً أي شيء ناقص** (`scripts/preflight.py`). لا مزيد من
الإقلاع الناقص: `run.bat` / `run_webui.bat` / `run.sh` يشغّلون الفحص أولاً؛
ويمكن تخطيه بـ `--preflight off` أو `VIRALCUTTER_SKIP_PREFLIGHT=1`.

## كيف تبقى قائمة الأمان محدّثة دائماً؟ 🛡️

يوتيوب **لا ينشر قائمة رسمية بالكلمات الممنوعة** — المنع سياقي ومفهومي، لذلك
الحماية طبقات، وليس قائمة كلمات فقط:

| الطبقة | ماذا تفعل | كيف تُحدَّث |
|---|---|---|
| **1. قائمة الكلمات المحلية** (`safety_filter.py` + `safety_blocklist.json`) | تحظر/تكتم الشتائم والتحريض المعروف | **v7.18: متعدد المصادر** — كل تثبيت يدمج الحزم من المصدر الرسمي + مرايا المجتمع (أعلى خطورة تفوز لكل كلمة)، مع وضع مراقبة مستمرة `--watch 6` وزر "تحديث" في الواجهة، أو `python -m scripts.safety_updater --force` |
| **2. مراقب سياسات يوتيوب** (`youtube_policy_watch.py` + `youtube-policy-watch.yml`) | يفحص صفحات يوتيوب الرسمية (خطاب الكراهية، المضايقة، العنف، إرشادات المجتمع) يومياً؛ عند تغيّر أي صفحة يفتح تذكيراً تلقائياً لتحديث القائمة | تلقائي بالكامل — كل يوم (09:00 UTC) + يدوياً من تبويب الإجراءات |
| **3. مراجعة سياقية بالذكاء الاصطناعي** (`safety_ai.py`) | تلتقط المخالفة **حتى لو الكلمة غير مدرجة أصلاً** (مثل "هؤلاء لا يستحقون الوجود") | بلا تحديث — تعمل بذكاء النموذج نفسه (Gemini/G4F) |
| **4. حلقة الـ Strikes** (`strike_feedback.py`) | تتعلم كلمات المقاطع التي أخذت ضربة فعلاً وتحظرها | `python -m scripts.strike_feedback add --term "..."` أو من الواجهة |
| **5. بوابة الرفع** (`upload_gate.py` + بطاقة المخاطر) | تمنع المقطع الخطير قبل وصوله ليوتيوب نهائياً | بلا تحديث — فحص إجباري قبل أي رفع |

**متى أضيف كلمة جديدة للقائمة الكنسية (لكل المستخدمين):**
1. أضف الكلمة إلى قائمة `BLOCKLIST` في `scripts/safety_filter.py`
2. صدّر الحزمة برقم إصدار جديد: `python scripts/export_blocklist_pack.py --version 3`
3. ادفع إلى المستودع — ويصل التحديث لكل التثبيتات خلال 24 ساعة

**تذكير تلقائي**: سير عمل مرتين أسبوعياً (الاثنين + الخميس) في GitHub يفتح
Issue تلقائياً إذا مرّت 14 يوماً بلا تحديث، ويغلقه بنفسه عندما يُدفع إصدار
جديد.

> ⚠️ واقعي: لا أداة تضمن صفر مخالفات — يوتيوب يقيّم السياق، والمراجعة اليدوية
> للمقاطع مرتفعة الخطورة (المحجوبة في بطاقة المخاطر) قبل الرفع تبقى القرار الأخير.


## 🚫 SponsorBlock — تخطي الإعلانات المدمجة (v7.19)

عند التنزيل، يمكن إزالة مقاطع الرعاية والإعلانات المدمجة من الفيديو قبل
التقطيع (خيار `--sponsorblock` أو القائمة في الواجهة) — فيخرج المقطع القصير
نظيفاً من قراءات الإعلانات. مدعوم عبر yt-dlp دون أي اعتماد إضافي.

## 🔴 تحميل البث المباشر تلقائياً (v7.20)

ضع رابط بث مباشر (مثل `https://youtube.com/live/gowpuk4jk5U`) وسينتظر
البرنامج انتهاء البث ثم يحمّله كفيديو عادي ويكمل المعالجة (تقطيع + ترجمات +
نشر):

- `python scripts/download_live.py <URL> --check` — اعرف حالة البث فقط
- `python scripts/download_live.py <URL> --max-wait 360` — انتظر حتى 6 ساعات ثم نزّل
- من الواجهة: حقل **"🔴 Live stream: wait for end (minutes)"** بجانب رابط
  يوتيوب — يعمل للبث المجدول والمباشر على حد سواء.

## 📚 المعجم العربي المسيء — قاعدة بيانات تتحدث تلقائياً (v7.19)

دمجنا **المعجم الأكاديمي للكلمات المسيئة العربية** (إعداد محمد عطية، Google
Research — مبني على مجموعات موسومة يدوياً من وسائل التواصل) كمصدر ثالث
للقاعدة: 148 مصطلحاً عربياً جديداً (شتائم هوية، تحقير، عبارات كراهية
للجنسين). القاعدة الكنسية الآن **296 مصطلحاً** (v4).

- **تتحدث باستمرار**: `python scripts/arabic_lexicon_importer.py --merge-blocklist`
  يسحب أحدث نسخة من المستودع الأم ويضيف الكلمات الجديدة، وسير عمل أسبوعي
  (`arabic-lexicon-freshness.yml`) يفتح تذكيراً تلقائياً عند تغيّر المصدر.
- **إصلاح البادئات**: الكلمات المسبوقة بو/ف (وخولات، فاذبحوهم) تُكتشف الآن
  بدقة دون إيجابيات كاذبة على الكلام العادي.

## 🚫 محرك الأصالة — حماية المحتوى المكرر (v7.18)

يوتيوب يفرض "سياسة المحتوى المُعاد استخدامه": إعادة نشر المقطع نفسه (حتى بعد
إعادة الترميز) تعرّض القناة لفقدان التمويل أو مخالفة. الحل طبقتان:

| الأداة | ماذا تفعل |
|---|---|
| **بصمة بصرية إدراكية** (`scripts/originality.py compare`) | كل مقطع يُختزل إلى بصمة 16 إطاراً؛ مقارنة مقطعين تُظهر نسبة التشابه الفعلية — والتشابه ≥ 80% يُمنع تلقائياً |
| **حارس المحتوى** (`content_guard.py`) | عند كل نشر يُحفظ `visual_fingerprint`؛ أي مقطع جديد مطابق بصرياً لنفس المصدر يُحظر برمز `perceptual_near_duplicate` قبل الرفع |
| **تحويلات حتمية** (`transform_with_seed`) | لإعادة قصّ المقطع نفسه بشكل مختلف لكل منصة: سرعة دقيقة، قلب أفقي، إزاحة قصّ، تدرّج لوني — تغييرات تحريرية حقيقية ببذرة ثابتة قابلة للتكرار |
| **تقرير الأصالة** (`originality_report.json`) | لكل مشروع: ملخص المكرر/المتشابه/المختلف قبل النشر |

> ✍️ ملاحظة صادقة: هذه الأدوات تجعل **محتواك المحرَّر** فريداً وتجنّبك
> إعادة نشر المقطع نفسه — لا تُغني عن حقوقك في استخدام محتوى الآخرين.


## المساهمة

> 📋 **للمطوّرين**: اقرأ [تقرير تسليم المطور](docs/DEVELOPER_HANDOVER.md) أولاً — يشرح ما بُني، كيف يعمل، وما المتبقي للعمل عليه.  
> 📑 **خارطة الطريق الكاملة**: [التقرير الشامل لما يجب تطويره](docs/ROADMAP_REPORT.md) — كل المهام مرتّبة بالأولوية مع الجهد والأثر وخطة التنفيذ.
> 👁️ **الفحص البصري الآمن**: [دليل الفحص البصري للمحتوى الحساس](docs/VISUAL_SAFETY_AR.md) — التفعيل من Windows وWebUI، سياسة الحظر، الاختبارات، وحدود الكشف.
> 🔐 **الملفات وYouTube OAuth**: [دليل اختيار الملفات وتسجيل الدخول إلى YouTube](docs/YOUTUBE_AND_FILE_PICKERS_AR.md) — اختيار الشعار والموسيقى وB-Roll وSFX، حفظ واستبدال `client_secrets.json`، وتسجيل الدخول الآمن.
> ▶️ **إصلاح تنزيل YouTube**: [دليل 429/403 وDeno وyt-dlp-ejs](docs/YOUTUBE_DOWNLOAD_FIX_AR.md) — فصل الترجمة عن الفيديو، fallback للصيغ، cookies، وإصلاح مفتاح Gemini.

- **GitHub**: ضع ⭐ إذا أفادك المشروع!
- المشروع في هذا المستودع تطوير مستمر — الملاحظات والمشاكل عبر Issues.

## أدوات احترافية (v6.13–v6.14)

- 🧠 **علّم الأداة** (حلقة الضربات): تبويب "🧠 علّم الأداة" في الواجهة، أو `python -m scripts.strike_feedback add --term "..."` / `from-scorecard --project VIRALS/x --apply` — الأداة تتعلم كلمات المقاطع التي أخذت ضربة وتحظرها بعدها.
- 📈 **الأداء**: تبويب "📈 الأداء" في الواجهة أو `python -m scripts.analytics --summary|--top|--trends` (تحليلات يوتيوب، قراءة فقط — فعّل الـ API في كونسول جوجل أولاً).
- 📐 **أبعاد الإخراج**: `--output-aspect 4:5|1:1|16:9` (بعد حرق الترجمات) — أو قائمة "📐 تأطير الإخراج" في الواجهة.
- ✅ **تثبيت قابل للتكرار**: `uv sync` (يستخدم `uv.lock`)؛ تدفق `install_dependencies.bat` الكلاسيكي يعمل كما هو.


**الإصدار الحالي**: 7.26.0-pro — Audio QC محلي عبر FFmpeg، Telegram آمن، وfaster-whisper fallback
*OUSSAMA Cutter: لأن المقاطع الفيروسية لا يجب أن تكلّف ثروة.*
