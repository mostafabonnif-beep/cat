# ====================================================================
# ملف المتغيّرات البيئية النموذجي — OUSSAMA Cutter / ViralCutter
# --------------------------------------------------------------------
# انسخ هذا الملف إلى .env ثم املأ القيم المطلوبة:
#     cp .env.example .env
#
# ⚠️  لا تضع أي مفتاح حقيقي في هذا الملف — .env.example يُرفَع إلى Git،
#     أمّا ملف .env فيجب أن يبقى محلياً ومُدرَجاً في .gitignore.
#
# المتغيّرات هنا مُستخرَجة آلياً من كود Python في المستودع (61 متغيّراً).
# ====================================================================

# --------------------------------------------------------------------
# 1) مفاتيح مزوّدي الذكاء الاصطناعي (AI / LLM)
# --------------------------------------------------------------------
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 12 موضعاً بالكود
GEMINI_API_KEY=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 8 موضعاً بالكود
VIRALCUTTER_GEMINI_KEYS=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 6 موضعاً بالكود
OPENAI_API_KEY=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 6 موضعاً بالكود
OPENAI_MODERATION_API_KEY=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 4 موضعاً بالكود
VIRALCUTTER_GEMINI_KEY=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_GEMINI_KEY_MODE=

# --------------------------------------------------------------------
# 2) يوتيوب والنشر (YouTube / Upload)
# --------------------------------------------------------------------
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 8 موضعاً بالكود
YT_CLIENT_SECRETS_FILE=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 8 موضعاً بالكود
YT_TOKEN_FILE=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_UPLOAD_THUMBNAIL=
# مُستخدَم في 2 موضعاً بالكود
YT_CATEGORY_ID=
# مُستخدَم في 2 موضعاً بالكود
YT_PRIVACY=

# --------------------------------------------------------------------
# 3) النسخ الصوتي و Whisper (Transcription)
# --------------------------------------------------------------------
# مُستخدَم في 8 موضعاً بالكود
VIRALCUTTER_FASTER_WHISPER_COMPUTE_TYPE=int8
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_TRANSCRIPTION_BACKEND=

# --------------------------------------------------------------------
# 4) السلامة والفلترة والرقابة (Safety / Moderation)
# --------------------------------------------------------------------
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_DISABLE_SAFETY_WATCHER=

# --------------------------------------------------------------------
# 5) الأسرار والتشفير (Secrets / Crypto)
# --------------------------------------------------------------------
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 6 موضعاً بالكود
VIRALCUTTER_CONFIG_PASSPHRASE=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 4 موضعاً بالكود
IG_ACCESS_TOKEN=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 4 موضعاً بالكود
TIKTOK_CLIENT_SECRET=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 2 موضعاً بالكود
HF_TOKEN=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 2 موضعاً بالكود
IG_CLIENT_SECRET=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 2 موضعاً بالكود
PYANNOTE_AUTH_TOKEN=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_WEBUI_PASSWORD=

# --------------------------------------------------------------------
# 7) المسارات والملفات (Paths / Files)
# --------------------------------------------------------------------
# مُستخدَم في 5 موضعاً بالكود
PATH=
# مُستخدَم في 4 موضعاً بالكود
GITHUB_OUTPUT=
# مُستخدَم في 2 موضعاً بالكود
TMP=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_DENO_PATH=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_EXTRA_STATIC_DIRS=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_VIRALS_DIR=

# --------------------------------------------------------------------
# 8) التشخيص والتسجيل (Debug / Logging)
# --------------------------------------------------------------------
# قيمة منطقية: true / false — مُستخدَم في 6 موضعاً بالكود
VIRALCUTTER_DEBUG_FACES=false
# قيمة منطقية: true / false — مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_VERBOSE=false
# مُستخدَم في 1 موضع بالكود
ORT_LOGGING_LEVEL=
# مُستخدَم في 1 موضع بالكود
TF_CPP_MIN_LOG_LEVEL=

# --------------------------------------------------------------------
# 9) إعدادات أخرى (Misc)
# --------------------------------------------------------------------
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 4 موضعاً بالكود
ACOUSTID_API_KEY=
# مُستخدَم في 4 موضعاً بالكود
OUSSAMA_CONTENT_REGISTRY=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 4 موضعاً بالكود
PEXELS_API_KEY=
# سرّي — لا تُرفَع قيمته إلى Git إطلاقاً — مُستخدَم في 4 موضعاً بالكود
TIKTOK_CLIENT_KEY=
# مُستخدَم في 4 موضعاً بالكود
VIRALCUTTER_SKIP_PREFLIGHT=
# مُستخدَم في 3 موضعاً بالكود
VIRALCUTTER_ALLOW_PLACEHOLDER=
# مُستخدَم في 2 موضعاً بالكود
GRADIO_SERVER_PORT=
# مُستخدَم في 2 موضعاً بالكود
IG_CLIENT_ID=
# مُستخدَم في 2 موضعاً بالكود
IG_HOST_DISABLE=
# مُستخدَم في 2 موضعاً بالكود
IG_SHARE_TO_FEED=
# مُستخدَم في 2 موضعاً بالكود
IG_USER_ID=
# مُستخدَم في 2 موضعاً بالكود
IG_VIDEO_URL=
# مُستخدَم في 2 موضعاً بالكود
OUSSAMA_POLICY_DB=
# مُستخدَم في 2 موضعاً بالكود
TEMP=
# مُستخدَم في 2 موضعاً بالكود
TIKTOK_PRIVACY=
# مُستخدَم في 2 موضعاً بالكود
TIKTOK_REDIRECT_URI=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_ALLOW_UNSAFE_LOAD=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_ALLOW_UNSIGNED_UPDATE=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_CRASH_ENDPOINT=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_CRASH_REPORT=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_HOST=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_LANG=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_MIN_FINAL_SCORE=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_QUALITY_GATE=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_REPETITION_PENALTY_MAX=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_SEMANTIC_DUP_THRESHOLD=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_STRICT_BOUNDARIES=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_VARIANT_VERIFY=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_VOICE_FACE_LINK=
# مُستخدَم في 2 موضعاً بالكود
VIRALCUTTER_WEBUI_USER=
